---
tags: [算法, 动态规划, 栈, 队列, 链表]
---

## 数组

```antd
const { NavList } = components;
const data = [{
	title: '删除有序数组中的重复项',
	desc: '删除有序数组中的重复项'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```

## 栈

```antd
const { NavList } = components;
const data = [{
	title: '包含min函数的栈',
	desc: '包含min函数的栈'
}, {
	title: '反转链表',
	desc: '利用栈来反转链表'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```

## 队列

## 链表

```antd
const { NavList } = components;
const data = [{
	title: '旋转链表',
	desc: '旋转链表'
}, {
	title: '删除排序链表中的重复元素II',
	desc: '删除排序链表中的重复元素II'
}, {
	title: '反转链表',
	desc: '反转链表'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```

## 哈希表

```antd
const { NavList } = components;
const data = [{
	title: '两数之和',
	desc: '两数之和'
}, {
	title: '字母异位词分组',
	desc: '字母异位词分组'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```

## 二分查找

```antd
const { NavList } = components;
const data = [{
	title: 'x的平方根',
	desc: '牛顿法求 x 的平方根'
}, {
	title: '搜索旋转排序数组II',
	desc: '搜索旋转排序数组II'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```

## 双指针

```antd
const { NavList } = components;
const data = [{
	title: '两数之和',
	desc: '两数之和'
}, {
	title: '三数之和',
	desc: '三数之和'
}, {
	title: '盛水最多的容器',
	desc: '盛水最多的容器'
}, {
	title: '合并区间',
	desc: '合并区间'
}, {
	title: '删除有序数组中的重复项',
	desc: '删除有序数组中的重复项'
}, {
	title: '外观数列',
	desc: '外观数列'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```

## 滑动窗口

## 字符串

```antd
const { NavList } = components;
const data = [{
	title: '罗马数字与数字的转换',
	desc: '罗马数字与数字的转换'
}, {
	title: '最长回文字符串',
	desc: '最长回文字符串'
}, {
	title: '简化路径',
	desc: '简化路径'
}, {
	title: '复原IP地址',
	desc: '复原IP地址'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```

## 分而治之

```antd
const { NavList } = components;
const data = [{
	title: '合并K个升序链表',
	desc: '合并K个升序链表'
}, {
	title: 'Pow(x,n)',
	desc: 'Pow(x,n)'
}, {
	title: '不同的二叉搜索树II',
	desc: '不同的二叉搜索树II'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```

## 动态规划

```antd
const { NavList } = components;
const data = [{
	title: '跳跃游戏II',
	desc: '跳跃游戏II'
}, {
	title: '最大子序和',
	desc: '最大子序和'
}, {
	title: '不同路径',
	desc: '不同路径'
}, {
	title: '最小路径和',
	desc: '最小路径和'
}, {
	title: '解码方法',
	desc: '解码方法'
}, {
	title: '不同的二叉搜索树',
	desc: '不同的二叉搜索树'
}, {
	title: '交错字符串',
	desc: '交错字符串'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```

## 贪心

## DFS

```antd
const { NavList } = components;
const data = [{
	title: '全排列',
	desc: '全排列'
}, {
	title: '组合总数',
	desc: '组合总数'
}, {
	title: '组合',
	desc: '组合'
}, {
	title: '单词搜索',
	desc: '单词搜索'
}, {
	title: '子集',
	desc: '子集'
}];
ReactDOM.createRoot(el).render(<NavList data={data} />);
```
